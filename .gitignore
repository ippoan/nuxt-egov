# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# e-Gov spec files (downloaded via egov-spec skill)
spec/

# Claude Code
.claude/*
!.claude/skills/

# Downloaded schema files
kousei_schema/
.wrangler/

# dev dist-tag 追従のため lockfile は commit しない (commit すると npm install が
# dist-tag を再解決せず古い版に固定される — ippoan/egov-shinsei-sdk#29 で実害)
package-lock.json
